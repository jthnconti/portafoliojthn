name: Deploy to GitHub Pages
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/configure-pages@v1
      - run: echo "Hello World" > docs/index.html  # Reemplaza con tu build real
      - uses: actions/deploy-pages@v1